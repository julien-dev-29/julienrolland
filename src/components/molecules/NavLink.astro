---
type Props = {
  href: string;
  label: string;
  active: boolean;
};

const { href, label, active } = Astro.props;
---

<li>
  <a
    href={href}
    class={`text-sm transition-colors duration-200 ${active ? "text-teal-400" : "text-teal-300"}`}>{label}</a
  >
</li>
